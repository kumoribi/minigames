# ミニゲーム集 機能仕様書

## 1. 文書情報

- 対象: `minigames`
- 基準実装: 2026-09-20 時点の `main`
- 公開先: <https://kumoribi.github.io/minigames/>
- 目的: 現在の実装仕様を明文化し、改修・回帰確認・ゲーム追加時の基準とする

本書は将来構想ではなく、原則として現在の実装を記述する。未実装事項は「既知の制約」に分離する。

## 2. システム概要

### 2.1 目的

スマートフォンの隙間時間に短時間で遊べる、インストール不要のミニゲーム集を提供する。各ゲームはクライアント側の HTML、CSS、JavaScript のみで動作し、サーバー処理や利用者登録を必要としない。

### 2.2 収録ゲーム

| ゲーム | プレイ形式 | 主な入力 |
|---|---|---|
| オセロ | プレイヤー対CPU | タップ／クリック |
| ポーションソート | 1人用パズル | タップ／クリック |
| マインスイーパー | 1人用パズル | タップ、長押し／クリック |
| 2048 | 1人用パズル | スワイプ、矢印キー |
| 麻雀ソリティア | 1人用パズル | タップ／クリック |
| ソナーハント | 1人用推理ゲーム | タップ／クリック |
| ナイトサバイバー | 1人用サバイバルアクション | スライド／キーボード |

### 2.3 技術構成

- 静的ファイルのみで構成する。
- 外部JavaScriptライブラリ、データベース、バックエンドAPIは使用しない。
- GitHub Pagesで公開する。
- トップページでService Workerを登録し、主要ファイルをキャッシュする。
- レイアウトはスマートフォンとPCの両方に対応する。

## 3. 共通仕様

### 3.1 トップページ

- タイトル、説明、収録ゲームのカード一覧を表示する。
- 各カードを選択すると、対応するゲームのサブディレクトリへ遷移する。
- 各ゲーム画面にはトップページへ戻るリンクを設ける。
- カードにはゲーム名、短い説明、視覚的なアイコンを表示する。

### 3.2 操作と表示

- スマートフォンでは縦画面を基本とし、盤面や操作ボタンが画面幅に収まること。
- 主要ボタンはタップ操作を想定した大きさを確保する。
- ゲーム状態、手番、スコア、残数など、プレイ判断に必要な情報を画面に表示する。
- ゲーム終了時はダイアログを表示し、再プレイ手段を提供する。
- ランダム要素を持つゲームは、再開始時に原則として新しい配置を生成する。

### 3.3 データ保存

| キー | 対象 | 内容 |
|---|---|---|
| `2048-best` | 2048 | 過去最高スコア |
| `mahjong-level` | 麻雀ソリティア | 最後に選択した難易度 |
| `sonar-best` | ソナーハント | 過去最高スコア |
| `night-survivor-best` | ナイトサバイバー | 過去最高スコア |

上記以外の盤面、経過時間、ステージ、対局結果はページ再読み込み後に保持しない。

## 4. ゲーム別仕様

### 4.1 オセロ

#### 4.1.1 基本ルール

- 盤面は8×8とする。
- 初期配置は中央4マスに黒2個、白2個を交互に置く。
- プレイヤーは黒、CPUは白とし、プレイヤーが先手となる。
- 相手の石を縦・横・斜め方向に自分の石で挟める空きマスを合法手とする。
- 石を置いた際、8方向で挟んだ相手の石をすべて自分の色へ反転する。

#### 4.1.2 手番と終了

- プレイヤーの手番では合法手を視覚的に表示する。
- 現在の手番に合法手がなく、相手に合法手がある場合は自動的にパスする。
- 両者とも合法手がない場合にゲームを終了する。
- 終了時に黒石・白石を数え、多い側を勝者とする。同数は引き分けとする。

#### 4.1.3 CPU

- CPUは合法手ごとに評価値を計算し、最も高い手を選択する。
- 評価には反転数、角、辺、着手後にプレイヤーへ与える合法手数を使用する。
- 角を最優先し、次に辺を優遇する。

#### 4.1.4 再開始

- 「最初から遊ぶ」または結果ダイアログの「もう一度遊ぶ」で初期状態へ戻す。
- 待機中のCPU処理は再開始時に取り消す。

### 4.2 ポーションソート

#### 4.2.1 盤面

- 1本のボトル容量は4層とする。
- ステージ番号を `n` とした場合、色数は `n + 4` 色とする。
- 色数と同数の充填済みボトルに、空ボトル2本を加える。
- 各色は必ず4層ずつ生成する。
- 色の並びとボトルの並びは開始ごとにランダム化する。

#### 4.2.2 移動ルール

- 移動元を選択し、続けて移動先を選択する。
- 空ボトル、または最上層が移動元の最上層と同じ色のボトルへ移動できる。
- 満杯のボトルへは移動できない。
- 移動元上部に同色が連続する場合、移動先の空き容量までまとめて移す。
- 同じボトルを再選択すると選択を解除する。

#### 4.2.3 クリアと操作

- すべての非空ボトルが同色4層になった時点でクリアとする。
- 有効な移動1回を1手として数える。
- 「1手戻す」は直前の有効な移動前の全ボトル状態へ戻す。
- 「やり直す」は同じステージ番号で新しい初期配置を生成し、手数を0へ戻す。
- クリア後の「次のステージへ」でステージを1増やし、色とボトルを1つずつ増やす。

### 4.3 マインスイーパー

#### 4.3.1 盤面

- 盤面は9×9の81マスとする。
- 地雷数は10個とする。
- 地雷は最初のマスを開いた時点でランダム配置する。
- 最初に開いたマスと、その周囲最大8マスには地雷を配置しない。

#### 4.3.2 操作

- マスをタップ／クリックすると開く。
- 500ミリ秒の長押しで未開封マスの旗を切り替える。
- 旗の立ったマスは通常操作では開かない。
- 周囲の地雷数が0のマスを開いた場合、隣接する未開封マスを連鎖的に開く。
- 地雷残数表示は `10 - 旗数` とする。旗数自体に上限は設けない。

#### 4.3.3 終了

- 地雷以外の71マスをすべて開くとクリアする。
- 地雷を開くと失敗し、すべての地雷を表示する。
- タイマーは初回操作から1秒単位で計測し、終了時に停止する。
- 再開始時は未配置状態の新しい盤面へ戻す。

### 4.4 2048

#### 4.4.1 盤面と生成

- 盤面は4×4とする。
- 開始時に数字タイルを2枚生成する。
- 新規タイルは空きマスからランダムに選び、90%で「2」、10%で「4」とする。
- 有効な移動後に新規タイルを1枚追加する。盤面が変化しない入力では追加しない。

#### 4.4.2 移動と合体

- スワイプまたは矢印キーで上下左右に移動する。
- スワイプは主軸方向に28px以上移動した場合に成立する。
- タイルは指定方向へ空きがなくなるまで移動する。
- 同じ数字が隣接した場合は合体し、値を2倍にする。
- 1回の移動中に生成されたタイルは、その移動中に再度合体しない。
- 連続入力時は処理中の最新1方向をキューに保持し、演出終了後に実行する。

#### 4.4.3 スコアと演出

- 合体後の数字をスコアへ加算する。
- 最高スコアはLocal Storageへ保存する。
- 移動は滑らかにアニメーションし、新規生成時と合体時に弾む演出を行う。
- 合体位置に光の粒と加算得点を表示し、対応端末では短く振動する。

#### 4.4.4 終了

- 2048以上のタイルを初めて生成したとき、達成ダイアログを約1.1秒表示してゲームを継続する。
- 全16マスが埋まり、上下左右に合体可能な組がない場合にゲームオーバーとする。
- 再開始時は現在スコアを0へ戻すが、最高スコアは保持する。

### 4.5 麻雀ソリティア

#### 4.5.1 目的と取得条件

- 同じ図柄の取得可能な牌を2枚選び、すべての牌を取り除くことを目的とする。
- 牌の上面に別の有効牌が重なっていないことを取得条件とする。
- 同じ層で左右の両側を牌に挟まれている牌は取得不可とする。
- 取得可能な牌のみ表面を表示し、取得不可の牌は裏面を表示する。
- 取得可能になった牌には反転して表になる演出を行う。

#### 4.5.2 山の生成

- 基礎層は四隅を除いた連結領域とし、山全体を一つにつなげる。
- 基礎層の上に複数の山を半牌単位でずらして積む。
- 上層の各牌は1つ下の層の牌と重なり、支えのない牌を作らない。
- 同じ層の牌同士を重ねない。
- 山の位置、段構成、図柄割り当てを新規ゲームごとにランダム化する。
- 取得可能牌を順次ペアとして除去できる順序をシミュレーションし、全牌へペアを割り当てられた配置だけを採用する。
- 6桁の山番号を表示するが、番号から同じ山を再現する機能は持たない。

#### 4.5.3 難易度

| 難易度 | 牌数 | 基礎グリッド | 山数 | 得点倍率 |
|---|---:|---:|---:|---:|
| かんたん | 36 | 6×4 | 2～3 | 1.0 |
| ふつう | 72 | 8×5 | 3～5 | 1.5 |
| むずかしい | 108 | 8×6 | 4～6 | 2.0 |

- 難易度を変更すると即座に新しい山を生成する。
- 選択した難易度はLocal Storageへ保存する。

#### 4.5.4 スコア、時間、操作

- 最初に牌を選択した時点でタイマーを開始する。
- 同じ図柄のペアを取ると、`100 × コンボ数 × 難易度倍率` を四捨五入した得点を加算する。
- 前回の成立から2.5秒以内に次のペアが成立するとコンボ数を増やす。それ以外は1へ戻す。
- ペア成立時に消去、リング、光粒、加算得点の演出を行う。
- 「ヒント」は現在取得可能な同一図柄ペアを強調する。存在しない場合はシャッフルを案内する。
- 「戻す」は直前に除去した1組を復元し、その手の直前の得点へ戻す。
- 「シャッフル」は残っている牌の位置を変えず、全除去可能な図柄割り当てへ変更する。
- 全牌を取り除くとタイマーを停止し、時間と得点を表示する。

### 4.6 ソナーハント

#### 4.6.1 目的と基本操作

- グリッド状の海域に隠れた潜水艦を、ソナーが示す距離から推理してすべて撃沈する。
- 「ソナー探索」と「爆雷投下」の2モードをボタンで切り替える。
- 海域の1マスを選ぶたびに、選択中の装備を1回消費する。
- 最初の海域操作時にタイマーを開始する。

#### 4.6.2 ソナー

- ソナー結果は、選択マスから全潜水艦セルのうち最も近いセルまでのマンハッタン距離とする。
- マンハッタン距離は行差の絶対値と列差の絶対値の合計とする。
- 結果が0なら選択マスの直下、1なら上下左右の隣接マスに潜水艦セルが存在する。
- 同じマスを再調査してもソナーを消費しない。
- ソナーを使い切った場合は自動的に爆雷モードへ切り替える。
- ソナー結果は爆雷投下後も盤面に保持する。
- 最新のソナー位置から結果と同じマンハッタン距離にあり、過去の全ソナー結果と矛盾しない未攻撃マスを潜水艦候補として強調する。
- 距離0の候補は黄色で強く表示し、候補、命中、外れの凡例を盤面下へ表示する。

#### 4.6.3 爆雷と撃沈

- 爆雷を投下したマスが潜水艦セルなら命中、それ以外は外れとする。
- 同じマスへ再投下しても爆雷を消費しない。
- 潜水艦を構成する全セルへ命中すると撃沈とする。
- 潜水艦は縦または横に直線で配置し、互いに重ならず、斜めを含む隣接もさせない。
- 爆雷を使い切る前に全艦を撃沈すると成功する。
- 全艦撃沈前に爆雷を使い切ると失敗し、未命中の潜水艦位置を表示する。
- 各海域の潜水艦を1隻ずつ、長さが分かる形で常時表示し、撃沈した艦は打ち消し表示にする。

#### 4.6.4 海域進行

| 海域 | 盤面 | 潜水艦の長さ | ソナー | 爆雷 |
|---|---:|---|---:|---:|
| 1 | 8×8 | 3、2、2 | 8 | 16 |
| 2 | 9×9 | 4、3、2、2 | 7 | 19 |
| 3 | 10×10 | 4、3、3、2 | 6 | 20 |
| 4以降 | 10×10 | 4、4、3、3、2 | 最低3まで段階減少 | 24 |

- 成功後の「次の海域へ」で海域番号を増やし、累計得点を維持する。
- 「最初から」または「最初から遊ぶ」で海域1、得点0から再開始する。
- 各海域の潜水艦位置は開始時にランダム生成する。

#### 4.6.5 得点と演出

- 潜水艦セルへの命中ごとに150点を加算する。
- 撃沈時は潜水艦の長さ×100点を追加する。
- 成功時は `残りソナー×80 + 残り爆雷×100 + 時間ボーナス + 海域×250` を加算する。
- 時間ボーナスは `max(0, 1200 - 経過秒×5)` とする。
- 最高累計得点をLocal Storageへ保存する。
- ソナー使用時は波紋、爆雷使用時は閃光演出を表示する。
- 盤面の行と列を均等な正方形に固定し、数字、命中記号、外れ記号はサイズ計算に影響しない重ね表示とする。

### 4.7 ナイトサバイバー

#### 4.7.1 目的と操作

- 自動攻撃を行うプレイヤーを移動させ、10分経過後に登場するボスを倒すとクリアとする。
- スマートフォンでは戦場内を押してスライドする仮想スティック、PCではWASDまたは矢印キーで移動する。
- 一時停止ボタン、Escapeキー、タブの非表示でゲームを停止できる。
- HPが0になるとゲームオーバーとする。

#### 4.7.2 敵と進行

- 通常敵はスライム、コウモリ、ゴーレムの3種類とし、2分ごとに出現数と種類を上昇させる。
- 通常敵のHP倍率は、経過分を `m` として `(1 + 0.4m)^2` とし、2分で約3.2倍、5分で9倍、10分で25倍まで連続的に増加させる。
- 通常敵は10%から25%の確率で強化個体となる。出現率は1分ごとに1.5ポイント上昇し、強化個体はHP5倍、攻撃力1.5倍、速度1.08倍、経験値3倍、得点4倍とする。
- 強化個体は通常より18%大きく、金色の発光・輪郭・HPバーを表示する。
- 敵は一度画面内へ入るまで攻撃対象にならず、攻撃やアイテムによる画面外撃破を防止する。
- 45秒後から70秒間隔でエリートを出現させる。
- エリートのHPは1分ごとに12%増加し、ボスの基礎HPは固定とする。
- 通常生成に加え、包囲、一方向、円形、隙間のある壁状の4種類のウェーブを発生させる。
- 10分経過時に通常敵の出現を止め、ボスを出現させる。
- 敵との接触でダメージを受け、被弾後は0.65秒の無敵時間を設ける。
- 敵数は最大180体、弾は最大120個、経験値は最大400個、パーティクルは最大140個とする。
- 敵の当たり判定は空間グリッドで候補を絞り、遠距離の敵は更新頻度を下げ、弾とパーティクルは再利用する。

#### 4.7.3 攻撃と強化

- 初期武器は最も近い敵を狙う魔法弾とする。
- 武器は魔法弾、回転刃、雷撃、炎の輪、貫通矢、氷結波、聖なる光の7種類とし、同時所持は4種類、各最大レベルは5とする。
- すべての武器は固有の射程を持ち、射程外の敵を攻撃しない。攻撃範囲能力は全武器の射程をレベルごとに12%拡大する。
- 能力は攻撃力、移動速度、攻撃速度、攻撃範囲、吸引範囲、最大HP、自動回復、防御力の8種類とし、同時所持は4種類、各最大レベルは5とする。
- 敵を倒すと経験値を落とし、必要量を集めるとレベルアップする。
- 初回必要経験値は24とし、以降は `round(24 + レベル×13 + レベル^1.5)` で増加させる。
- 装備欄は武器と能力を分け、それぞれ現在数、上限4、空き枠を表示する。
- レベルアップ中は戦闘を停止し、取得可能な強化から3候補を表示する。候補には武器・能力の種別を色分けして明示し、取得済み項目を最大2件優先して成長中の構成を育てやすくする。
- レベルアップごとに候補を1回だけ引き直せる。最大レベルの項目は候補に含めない。
- 武器または能力が4枠埋まった状態でも同種の未取得項目を候補に表示し、選択した場合は現在所持する同種4項目から1つを指定して入れ替える。
- 入れ替え後の新項目はLv1とし、外した項目のレベルは失う。最大HPを外した場合は上昇分を戻し、進化済み武器を外した場合は進化状態も解除する。
- 進化成立後に必要能力を入れ替えても、既に成立した武器進化は維持する。

#### 4.7.4 武器進化

- 宝箱取得時、条件を満たす進化を通常強化より優先して1件発生させる。
- 進化した武器は元の武器枠を引き継ぎ、装備欄で進化名と専用表示へ切り替える。
- ゲーム内の説明欄に全進化の武器、必要能力、進化後名称を表示する。

| 武器Lv5 | 必要能力Lv3 | 進化名 |
|---|---|---|
| 魔法弾 | 攻撃力 | 星砕きの魔弾 |
| 回転刃 | 防御力 | 守護輪刃 |
| 雷撃 | 攻撃速度 | 天雷連鎖 |
| 炎の輪 | 攻撃範囲 | 獄炎結界 |
| 貫通矢 | 移動速度 | 幻影連弩 |
| 氷結波 | 吸引範囲 | 絶対零域 |
| 聖なる光 | 自動回復 | 暁の聖域 |

#### 4.7.5 アイテム

- 通常敵は低確率で回復薬、磁石、爆弾、時計を落とし、エリートは必ず宝箱を落とす。
- 回復薬はHP回復、磁石は全経験値回収、爆弾は全敵へ大ダメージ、時計は敵を4秒停止させる。
- 宝箱取得時は戦闘を停止し、獲得した強化または進化の名称と効果を専用画面に表示する。
- 進化条件を満たさない宝箱は、取得済み項目を優先して有効な強化を1件ずつ再抽選し、最大2件付与する。
- 宝箱による自動強化では、所持中の武器・能力を入れ替えない。
- 全項目が完成している場合、宝箱はHPを全回復する。

#### 4.7.6 得点と保存

- 通常敵、エリート、ボスは種類別の撃破点を持ち、結果画面ではエリート撃破数も表示する。
- 生存1秒につき5点を加算し、ボス撃破時は5000点を追加する。
- 最高得点はLocal Storageの `night-survivor-best` へ保存する。
- ゲーム終了時はレベル、撃破数、得点を表示し、再プレイ手段を提供する。

## 5. PWA・オフライン仕様

- Web App Manifestの名称は「ミニゲーム集」、表示モードは `standalone` とする。
- 開始URLはトップページとする。
- Service Workerのインストール時にトップページ、共通CSS、全ゲームのHTML/CSS/JavaScriptをキャッシュする。
- ナビゲーション要求はネットワークを優先し、失敗時にキャッシュを使用する。
- その他の静的ファイル要求はキャッシュを優先し、未保存の場合にネットワークを使用する。
- キャッシュ名を更新することで旧キャッシュを削除し、新版へ切り替える。

## 6. フォルダ構成

```text
minigames/
├── index.html
├── manifest.webmanifest
├── service-worker.js
├── SPECIFICATION.md
├── TEST_SPECIFICATION.md
├── styles/
│   └── common.css
├── othello/
├── potion-sort/
├── minesweeper/
├── 2048/
├── mahjong-solitaire/
├── sonar-hunt/
└── night-survivor/
```

各ゲームフォルダは原則として `index.html`、`style.css`、`game.js` を持つ。ゲーム固有の追加スタイルや素材は同じゲームフォルダ内へ置く。

## 7. 新規ゲーム追加仕様

1. 英小文字とハイフンでゲーム用サブディレクトリを作成する。
2. 単体で起動できる `index.html`、`style.css`、`game.js` を配置する。
3. ゲーム画面から `../` でトップページへ戻れるようにする。
4. スマートフォンの320px幅以上で横スクロールせず操作できるようにする。
5. タップ対象には十分な操作領域を確保し、状態を色だけに依存させない。
6. トップページへゲームカードを追加する。
7. Service Workerのキャッシュ対象へ必要ファイルを追加し、キャッシュ名を更新する。
8. 本仕様書とテスト仕様書へゲーム固有仕様・テストを追加する。
9. ローカル確認後、`minigames`だけを分離してGitHubへ公開する。

## 8. 既知の制約

- Manifestにアプリアイコンが未登録のため、端末によってはホーム画面追加時に汎用アイコンとなる。
- Service Workerはトップページを一度オンラインで開き、登録とキャッシュ保存が完了した後にオフライン利用できる。
- ゲーム途中の盤面は保存しない。
- 麻雀ソリティアのサウンドボタンは画面に存在するが、音声再生・切り替え処理は未実装である。
- 麻雀ソリティアの山番号は表示識別用であり、シード値としては使用しない。
- ポーションソートはステージ進行を再読み込み後に保持しない。
